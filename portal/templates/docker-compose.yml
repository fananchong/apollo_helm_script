# Code generated by helm. DO NOT EDIT!
version: '2'

services:
  apollo-portal:
    image: apolloconfig/apollo-portal:{{ .Values.apollo_version }}
    container_name: apollo-portal
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://apollo-db:3306/ApolloPortalDB?characterEncoding=utf8&serverTimezone=Asia/Shanghai
      SPRING_DATASOURCE_USERNAME: {{ .Values.portal_db_user }}
      SPRING_DATASOURCE_PASSWORD: {{ .Values.portal_db_passwd }}
    restart: always
    depends_on:
      - apollo-db
    ports:
      - "{{ .Values.portal_port }}:8070"
    volumes:
      - "{{ .Values.portal_log_dir }}:/opt/logs"
    links:
      - apollo-db

  apollo-db:
    image: mysql:5.7
    container_name: apollo-portal-db
    environment:
      TZ: Asia/Shanghai
      MYSQL_ROOT_PASSWORD: {{ .Values.portal_db_passwd }}
    restart: always
    ports:
      - "{{ .Values.portal_db_port }}:3306"
    volumes:
      - ./sql:/docker-entrypoint-initdb.d
      - "{{ .Values.portal_db_dir }}:/var/lib/mysql"
